dubbo:
  application:
    logger: slf4j
    name: DubboNacosServiceApplication
  registry:
    address: nacos://${nacos.address:127.0.0.1}:8848
  protocol:
    name: tri
    port: 50051
server:
  port: 8080
